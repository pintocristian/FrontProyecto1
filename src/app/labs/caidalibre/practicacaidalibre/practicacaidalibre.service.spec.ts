/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { PracticacaidalibreService } from './practicacaidalibre.service';

describe('Service: Practicacaidalibre', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PracticacaidalibreService]
    });
  });

  it('should ...', inject([PracticacaidalibreService], (service: PracticacaidalibreService) => {
    expect(service).toBeTruthy();
  }));
});
